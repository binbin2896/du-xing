---
name: du-xing
description: 笃行.skill — 执行落地。把结论翻译成"谁、哪天前、做什么、做到什么程度算完成"的可执行清单。
tags: [execution, planning, task-management, checklist]
---

# 笃行.skill — 执行落地

> 博学之，审问之，慎思之，明辨之，笃行之。 —— 《中庸》

知道要做什么了——**但"知道"和"做到"之间，还差一张可执行的清单。**

## 何时触发

当用户有以下需求时启用：

- **执行落地**：
- "知道了，具体怎么做"
- "帮我排一个执行计划"
- "下一步做什么"
- "落地"

**任务分配**：
- "谁来做这件事"
- "帮我把方案拆成步骤"

**承接上游**：
- 拷问评🟢的方案进入笃行
- 纲目/执要输出的行动需要具体执行

❌ 不触发：还没决定做什么（先走纲目/执要），纯信息查询

> 💡 **进入前校验**：执行前先问自己"能否一句话说清目标？"——如果说不清，说明还没想透，退回上游先明确目标再进笃行。

---

## 在绳墨生态中的位置

绳墨是思维工具链的组合入口（参见 `productivity/shengmo`）。本 skill 是链中的 **第四环（执行落地/最终输出）**：

```
钩玄(提炼) → 纲目/执要(判断) → 拷问(验证) → 笃行(执行)
                                                  ↑
                                              你在这里
```

- **上游**：拷问评🟢 的方案/判断 → 作为笃行的执行输入
- **特殊入口**：如果用户要求跳过拷问，纲目/执要的输出可直接进笃行
- **下游**：可执行清单（最终产出，无极下游）
- **独立调用**：已知道要做什么、需要落地步骤 → 直接调笃行
- **接入绳墨**：通过绳墨入口调用时，自动从拷问接过已质检通过的方案

## 和执要的区别

| 维度 | 执要 | 笃行 |
|:----|:----|:----|
| 核心问题 | 先做什么？ | 具体怎么做？ |
| 输出 | 排序清单 | 执行清单 |
| 粒度 | 事项级 | 步骤级 |
| 落地信息 | 截止时间 | 责任人+步骤+验收标准 |

## 输出格式（严格遵循）

### 默认模式（简单，3-5步）

```
┌─ 笃行清单 ─────────────────────────────────
│
│ 🎯 目标：[一句话说明要达成什么]
│ ✅ 完成标准：[什么指标、什么状态算做完]
│
│ ┌─────┬──────┬──────┬──────────┐
│ │ 步骤 │ 谁   │ 截止  │ 验收标准  │
│ ├─────┼──────┼──────┼──────────┤
│ │ 1.   │      │      │          │
│ │ 2.   │      │      │          │
│ │ 3.   │      │      │          │
│ └─────┴──────┴──────┴──────────┘
│
│ ⚠️ 风险预案
│ • 如果 [卡点] → 联系 [谁] 决定
│ • 如果 [延期] → 启动 [备选方案]
│
└────────────────────────────────────────────
```

### 单人任务模式（无"谁"列，风险自适应）

```
┌─ 笃行清单 · 单人 ───────────────────────────
│
│ 🎯 目标：[一句话]
│ ✅ 完成标准：[指标]
│
│ 步骤 | 截止    | 验收标准
│ ─────┼────────┼──────────
│ 1.   |        |
│ 2.   |        |
│ 3.   |        |
│
│ ⚠️ 风险预案
│ • 如果XX卡住了 → 可以做什么（自处理）
│ • 如果XX延期 → 调整YY的优先级
│
└────────────────────────────────────────────
```

### 复杂模式（子任务分支，用于大型项目）

当任务无法线性拆解（如"提升团队凝聚力"类复合目标），自动切换复杂模式：

```
┌─ 笃行清单 · 复杂 ───────────────────────────
│
│ 🎯 目标：[一句话]
│ ✅ 完成标准：[指标]
│
│ 主线：[核心路径]
│ ├─ 子任务1 | 谁 | 截止 | 验收
│ ├─ 子任务2 | 谁 | 截止 | 验收
│ └─ 子任务3 | 谁 | 截止 | 验收
│
│ 支线：[并行/辅助路径]
│ └─ ...
│
│ ⚠️ 风险预案（同上）
│
└────────────────────────────────────────────
```

## 执行步骤

### 第0步：执行前快照（安全阀）

在动手执行任何操作之前，先对将要修改的文件或目录拍一份快照。

**操作方法**（择一）：
- Git 项目：`git stash push -m "笃行快照 $(date +%Y%m%d_%H%M%S)"`
- 非 Git 项目：`cp -r <target_dir> <target_dir>.bak.$(date +%Y%m%d_%H%M%S)`

**快照范围**：当前步骤要改什么，就拍什么。不拍无关文件。

**执行完成后**：
- 如果一切正常 → 用户确认后删除快照（`git stash drop` 或 `rm -rf *.bak.*`）
- 如果出问题 → 自动执行回滚（`git stash pop` 或 `cp -r *.bak.* <target_dir>`）

> ⚠️ 如果用户明确说"不用备份，直接改"，可以跳过此步。

### 第1步：判断模式

根据输入自动判断使用哪种模式：
- **单人任务**（输入中只有"我""自己"，没有其他人名/角色）→ 单人模式
- **多人协作**（输入中有多个角色）→ 默认模式（有"谁"列）
- **复合目标**（任务无法线性拆解，如"提升XX""打造XX"）→ 复杂模式

### 第一步：拆步骤

把"要做的事"拆成可独立执行的步骤。
- 默认模式：3-5 步，每个步骤是一个完整动作
- 复杂模式：拆为主线+支线，子任务可独立追踪
- 如果超过5步，说明粒度太细；少于3步说明太粗

### 第二步：填信息

每个步骤填入：
- **谁**—具体人名或角色，不是"相关部门""安排人做"（单人模式跳过此列）
- **截止**—具体到日期，不是"尽快"
- **验收**—可以用数字、状态、可验证的事实衡量的标准

### 第三步：设风险

至少列两个风险预案，根据模式自适应：
- **团队任务**："如果XX卡住了 → 联系[谁]决定"
- **单人任务**："如果XX卡住了 → 可以自己查资料/调整方法"
- 只写真实会发生的风险，不写套话

## 风格要求

- 每步不超过一行描述
- 验收标准必须是可验证的（"报告写完并通过审核"✓ / "尽量提高质量"✗）
- 责任人必须具体到人
- 风险预案只写真实会发生的，不写套话
